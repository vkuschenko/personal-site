<style is:global>
  .accent {
    color: var(--clr-accent);
  }

  .animated-text,
  .animated-text:active,
  .animated-text:visited {
    text-decoration: none;
    color: var(--clr-text);
    font-size: 14px;
    font-weight: 500;

    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;

    background-image: linear-gradient(
      135deg,
      var(--clr-text) 0%,
      var(--clr-text) 40%,
      var(--clr-accent) 50%,
      var(--clr-text) 60%,
      var(--clr-text) 100%
    );
    background-position: 0% 0%;
    background-size: 400% 400%;
    animation: 2.5s ease-in-out 1s infinite normal none shine;
  }

  .animated-text:hover {
    color: var(--clr-accent);
    animation: none;
  }

  @keyframes shine {
    0% {
      background-position: 100% 100%;
    }
    100% {
      background-position: 0% 0%;
    }
  }
</style>
